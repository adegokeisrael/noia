[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --cov=noia
    --cov-report=term-missing
    --cov-report=html:htmlcov
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
